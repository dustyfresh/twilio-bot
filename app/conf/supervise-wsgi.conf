[program:wsgi]
command=/bin/bash -c 'source /opt/python3/venv/bin/activate && uwsgi --uid www-data --gid www-data -s /tmp/wsgi.sock --chdir /opt/app/ -w app --enable-threads'
autorestart=true
stdout_logfile = /opt/app/logs/wsgi.log
stderr_logfile = /opt/app/logs/wsgi.log
